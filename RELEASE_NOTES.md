# Release Notes — Harness

## [Unreleased]

### Added
- Initial project scaffold
- F01 · App Shell & Platform Bootstrap — PyWebView + FastAPI (127.0.0.1-only) + first-run wizard; KeyringGateway facade (IAPI-014 / IFR-006 with Linux plaintext fallback); ClaudeAuthDetector (IFR-001 credential-inheritance surface); BindGuard loopback enforcement; `/api/health` endpoint; zh-CN user-facing strings
- F02 · Persistence Core — SQLite `tickets` single-table + append-only JSONL audit log (IAPI-011 TicketRepository.save/get/list_by_run/list_unfinished/mark_interrupted + IAPI-009 AuditWriter.append); 9-state `TicketStateMachine` with legal-transition matrix (FR-006); `RunRepository` CRUD; `RecoveryScanner.scan_and_mark_interrupted` for NFR-005 crash recovery; `Schema.ensure` idempotent WAL migration (journal_mode=WAL, busy_timeout=5000ms, foreign_keys=ON); pydantic v2 Ticket aggregate with 7 sub-structures (FR-007); workdir-only write isolation under `<workdir>/.harness/` (NFR-006); aiosqlite 0.20 + structlog 24.4 pinned
- F10 · Environment Isolation & Skills Installer — per-run `.harness-workdir/<run-id>/.claude/` tree with `settings.json` / `mcp.json` + **physical-copy** of `plugins/longtaskforagent` bundle via `shutil.copytree(dirs_exist_ok=True)` + sha256 manifest verification (single-path, no `sys.platform` branch — supersedes original symlink design per 2026-04-24 user ruling, avoids Windows `WinError 1314`); `EnvironmentIsolator.setup_run` / `teardown_run` with nanosecond-precision `HomeMtimeGuard` snapshot+diff enforcing NFR-009 zero-write to `~/.claude/`; `WorkdirScopeGuard.assert_scope` enforcing FR-044 `.harness/` + `.harness-workdir/` only; `SkillsInstaller` with git URL whitelist (https + git@host:path, rejects file://, shell meta, `..`) + `run.lock` guard; REST `POST /api/skills/{install,pull}` (IAPI-018) with 400/409 error mapping; `PluginRegistry` manifest reader (≤64 KiB DoS cap) + idempotent `sync_bundle`; `AuditWriter.append_raw` non-destructive extension for `env.setup` / `env.teardown` / `skills.install` events (IAPI-009 reuse)
- F12 · Frontend Foundation — React 18.3 + Vite 5.4 + TypeScript 5.5 + Tailwind 3.4 + shadcn/ui 前端基座（**无 FR 业务**，只为 F21/F22 提供 import contracts）：`HarnessWsClient` multi-channel WebSocket 单例（4 态状态机 `closed/connecting/open/reconnecting` + 指数退避 1/2/4/8/16s + 60s 心跳 + channel 白名单）· `useWs` React hook（mount 订阅 / unmount 退订）· `createApiHook` TanStack Query 工厂（Zod runtime 校验，GET→useQuery / POST|PUT→useMutation）· `apiClient.fetch` 统一封装（`HttpError`/`ServerError`/`NetworkError` 归一）· `createSlice` Zustand 工厂（devtools + 重名 guard）· `AppShell`（QueryClientProvider + BrowserRouter + ErrorBoundary + 全局 WS 单例生命周期）· 5 shared primitives 从 prototype 视觉等价移植为 TS+Tailwind：`Sidebar`（1280≥展开 240px / 1024–1279 折叠 56px / 激活项 3px accent 左条 / HIL 徽标 18×18 琥珀底）、`PhaseStepper`（8 相圆点 + done/current/pending 三态 + current pulse 光环）、`TicketCard`（UCD §2.6 9 态 dot + tool chip）、`PageFrame`、`Icons`（40+ re-export from lucide-react 0.441，stroke-width 1.75）· `tokens.css` 从 `docs/design-bundle/eava2/project/styles/tokens.css` **byte-identical** 克隆 + UCD §2.5 中文排印扩展（`.hns-cn-body`/`.hns-cn-heading`）+ UCD §2.2 `prefers-reduced-motion` 降级分支；`scripts/check_tokens_fidelity.sh` 强制校验 `:root` 段零偏移；`scripts/check_source_lang.sh`+`check_source_lang.py` 源码 grep 守护 NFR-010（仅简体中文）
- F19 · Bk-Dispatch — Model Resolver & Classifier（单一 TDD 单元，合并原 F07 Model Override Resolver + F08 Classifier Service）覆盖 FR-019/020/021/022/023 + IFR-004：`ModelResolver.resolve` 4 层优先级链（per-ticket > per-skill > run-default > cli-default）+ `ProvenanceTag` 追溯（FR-019/020）；`ModelRulesStore` 原子写 `~/.harness/model_rules.json`（temp+rename + POSIX 0o600）+ `ModelRulesCorruptError` 守卫；`ClassifierService.classify` 永不抛语义（IAPI-010：所有失败路径归一为 `Verdict(backend="rule")` + audit warning，简化 F20 状态机）；`LlmBackend` httpx AsyncClient + `response_format=json_schema` strict（10s timeout per IFR-004 PERF）→ `ClassifierHttpError` / `ClassifierProtocolError` 内部抛后 `FallbackDecorator` 捕获 + `RuleBackend.decide` 兜底（FR-022 AC-1/2 + FR-023 AC-1/2）；`RuleBackend` 5 级判定链（context_overflow → rate_limit → permission_denied → exit_code=0 → skill_error），context_overflow 置顶以对齐 FR-022 AC-2；`ProviderPresets` GLM/MiniMax/OpenAI/custom 4 预设 + `validate_base_url` 严格 hostname 白名单（精确等价或 `.`-前缀 endswith；custom 拒私网/loopback/link-local/非 https，`SsrfBlockedError`，FR-021 AC-3 / ATS L182 SSRF）；`PromptStore.get/put` v1 append-only history（仅持久化 `{rev, saved_at, hash=sha256, summary=first_line_120_chars}`，full body 由 `current` 覆写，FR-033b diff 延至 v1.1）+ 32 KB 上限 `PromptValidationError`；REST 路由 `GET/PUT /api/settings/model_rules` · `GET/PUT /api/settings/classifier` · `POST /api/settings/classifier/test`（401 / connection_refused / dns_failure / timeout 四类归一 `TestConnectionResult`，ATS INT-025）· `GET/PUT /api/prompts/classifier`（IAPI-002 子路由，via `harness/api/{settings,prompts}.py` + `app.include_router`）；keyring 复用 `harness.auth.KeyringGateway`（IAPI-014），api_key 仅经 `ApiKeyRef(service, user)` 走 `ConfigStore` leak detector（NFR-008，杜绝明文落 config.json）；coverage line 98.37% / branch 84.62%；46 unit + 52 supplement = 98 tests across 14 files（含 3 integration: real loopback HTTP / real tmp_path FS / `keyring.backends.fail`）；ST cases 61（61 auto PASS — F19 ui:false，UI 类别由 F22 Fe-Config 承担）
- F20 · Bk-Loop — Run Orchestrator · Recovery · Subprocess（单一 TDD 单元，合并原 F06 Run Orchestrator + F09 Anomaly Recovery + F11 Subprocess Integrations）覆盖 FR-001/002/003/004/024/025/026/027/028/029/039/040/042/047/048 + NFR-003/004/015/016 + IFR-003：`harness.orchestrator` —— `RunOrchestrator.start_run/pause_run/cancel_run/skip_anomaly/force_abort_anomaly`（IAPI-002 Provider · 单 Run 主循环 + 14-skill 透传不硬编码白名单，FR-047）+ `_run_loop` 自主驱动（`PhaseRouteInvoker.invoke → TicketSupervisor.run_ticket → AnomalyClassifier.classify → RetryPolicy.next_delay`，软目标 5s 进 running，FR-001 AC-1）；`PhaseRouteInvoker` 调 `python scripts/phase_route.py --json` 子进程 + 30s timeout（IFR-003）+ `PhaseRouteResult(extra="ignore")` 松弛解析（缺字段补默认 / 新增字段忽略，NFR-015）+ exit≠0 暂停 + stdout 非 JSON `phase_route_parse_error` audit；`SignalFileWatcher` 包 `watchdog` Observer + `PatternMatchingEventHandler` 200ms 防抖 + `asyncio.Queue` 投递 + `RunControlBus.broadcast_signal` ≤2s 端到端延迟（FR-048）；`RunLock` 包 `filelock` + `<workdir>/.harness/run.lock` `acquire(timeout=0.5)` 失败 → `RunStartError(reason="already_running")` → 409 ALREADY_RUNNING（NFR-016 单 workdir 单 run 互斥）；`harness.recovery` —— `AnomalyClassifier.classify` 5 类 regex/启发式（context_overflow / rate_limit / network / skill_error / timeout）+ `RetryPolicy.next_delay` 纯函数序列（context_overflow `[0,0,0,None]` · rate_limit `[30,120,300,None]` · network `[0,60,None]`，NFR-003/004/FR-026）+ `Watchdog.arm` `asyncio.create_task` 倒计时 1800s SIGTERM→5s→SIGKILL（FR-027）+ `RetryCounter` per-skill 内存表 + `[CONTRACT-DEVIATION]` 首行 → `cls=skill_error` 直通 aborted 不重试 + 暂停（FR-028）；`harness.subprocess.git.GitTracker.begin/end` 调 `git rev-parse HEAD` + `git log --oneline <head_before>..<head_after>` 解析 commits[]（IAPI-013 · FR-042 · 非 git repo `GitError(code="not_a_repo")` 不破 ticket 流）；`harness.subprocess.validator.ValidatorRunner.run` 60s timeout 调 `scripts/validate_*.py --json <path>` 子进程 + `ValidationReport{ok, issues, script_exit_code, duration_ms}` + exit≠0 stderr 不被吞（FR-039/040 · IAPI-016）+ `ValidatorTimeout(500)` 边界守卫；REST 路由：`POST /api/runs/{start,pause,cancel}` · `POST /api/anomaly/{ticket}/{skip,force-abort}` · `GET /api/git/commits` · `GET /api/git/diff/{sha}` · `GET /api/files/tree` · `GET /api/files/read`（`PathTraversalError` 400 · `..` 穿越拒绝）· `POST /api/validate/{file}`；WebSocket 三 channel：`/ws/run/:id`（`RunPhaseChanged`/`TicketSpawned`/`TicketStateChanged`/`RunCompleted`）· `/ws/anomaly`（`AnomalyDetected`/`RetryScheduled`/`Escalated`）· `/ws/signal`（`SignalFileChanged`）envelope 严格对齐 §6.2.3；`RunControlBus.submit_command` 统一接收 `RunControlCommand{kind, target_ticket_id?}` 路由到 5 个 RunOrchestrator 方法（IAPI-019）；coverage line 91.79% / branch 81.04%（≥ 90/80）；51 pytest functions across 9 test files（含 9 INTG: real phase_route subprocess / real asyncio.sleep / real validate_features.py / real git CLI / real watchdog FS / 并发 filelock / real sqlite / real REST+WS）；ST cases 51（51 auto PASS — F20 ui:false，UI 表面由 F21/F22 承担）
- F21 · Fe-RunViews — RunOverview + HILInbox + TicketStream（单一 TDD 单元，合并原 F13 RunOverview+HILInbox + F14 TicketStream）覆盖 FR-010/030/031/034 + NFR-002/011 + IFR-007：纯 UI Consumer 特性，三路由 `/`、`/hil`、`/ticket-stream` 共享 stream-json 数据域 + ticket 模型 + WebSocket 客户端（消费 F02 ticket schema · F12 PageFrame/Sidebar/PhaseStepper/HarnessWsClient/createApiHook/tokens.css · F18 `/ws/hil` HilQuestion/HilAnswer · F20 RunStatus/RunControlBus/`/ws/run/:id`，无新契约出口）；`apps/ui/src/routes/run-overview/`（`RunOverviewPage` + `runOverviewReducer` 6 元素 phase stepper / cost 累加 / pause / cancel via IAPI-002）· `apps/ui/src/routes/hil-inbox/`（`HilInboxPage` + `deriveHilControl` 5 路控件派生纯函数：`radio` / `checkbox` / `textarea` / `radio_with_freeform` / `checkbox_with_freeform`，`InvalidHilQuestionError` 边界守卫；`submitHilAnswer` REST IAPI-002 + WS ack 双确认；React `textContent` / `{text}` 模板插槽强制无 `dangerouslySetInnerHTML` 防 XSS FR-031 SEC；HILCard local component byte-equiv prototype HILInbox.jsx 的 phase 色带 header + 状态机 `idle→submitting→submitted→answered/error`）· `apps/ui/src/routes/ticket-stream/`（`TicketStreamPage` 三栏 layout 左 ticket-list / 中 event-tree `@tanstack/react-virtual ^3.10.8` 行级虚拟化 / 右 inspector；`useTicketStreamFilters` URL `?state=&tool=&run_id=&parent=` 双向同步 react-router-dom 7.0.1 `useSearchParams`，非法 enum 自动 cleanup；`useInlineSearch` Ctrl/Cmd+F 焦点 + `<mark data-search-hit>` 高亮 + `preventDefault` 阻止浏览器原生 find；`usePauseScrollOnInteraction` 用户 wheel/touch/keyboard 触发 `paused-by-user` 暂停 auto-scroll-to-bottom，`[data-testid="auto-scroll-indicator"]` Live ↔ 已暂停 chip）；`--fg-on-accent: #06101E` token 抽离 + 三处 inline-style 替换（U1 inline check 修复，与 F12 tokens.css `.btn.primary` 一致 prototype 字面）；coverage line 94.96% / branch 82.93%（≥ 90/80）；vitest 27 文件 / 177 tests PASS；ST cases 24（8 FUNC + 3 BNDRY + 9 UI + 2 SEC + 2 PERF · 23 PASS + 1 Known-Gap visual-regression — ST-UI-021-009 prototype 三页 artboard PNG 未导出，与 F12 ST-UI-012-009 同源跨 feature UCD §7 SOP 缺口；其余 8 UI 用例覆盖 §VRC 全部关键元素 + chrome-devtools take_snapshot 三页 + Layer 1b 正向渲染）；validate_st_cases.py VALID · check_ats_coverage.py OK
- F22 · Fe-Config — SystemSettings + PromptsAndSkills + DocsAndROI + ProcessFiles + CommitHistory（Wave 2 单一 TDD 单元，合并原 F15+F16）覆盖 FR-032/033/035/038/039/041 + NFR-008 + IFR-004/005/006：纯前端 Consumer 5 路由 `/settings`、`/skills`、`/docs`、`/process-files`、`/commits`，消费 IAPI-002（14 REST 路由）+ IAPI-014（keyring 经 REST，明文不出后端）+ IAPI-016（validate 双层校验）+ IAPI-018（skills install/pull）—— `apps/ui/src/lib/zod-schemas.ts`（手写 14 Zod schema · 假设 datamodel-code-generator 自动生成 deferred）· `apps/ui/src/api/routes/{settings-general,skills-tree,prompts-classifier,files,git,validate}.ts` 经 F12 `createApiHook` 工厂构造 14 hook · 5 路由根 `apps/ui/src/routes/{system-settings,prompts-and-skills,docs-and-roi,process-files,commit-history}/index.tsx` + 14 子组件（`MaskedKeyInput` `KeyringFallbackBanner` `SkillTreeViewer` `MarkdownPreview` `TocPanel` `RoiDisabledButton` `ProcessFileForm` `CrossFieldErrorList` `CommitList` `DiffViewer` `BinaryDiffPlaceholder` `NotAGitRepoBanner` 等）；NFR-008 全链路 plaintext 隔离（MaskedKeyInput 显示 `***abc`，明文仅活在 mutation 请求体生命周期，gcTime:0 防 cache，DOM 全树 grep 0 匹配）；IFR-006 Linux 无 Secret Service daemon → keyrings.alt 降级 + `KeyringFallbackBanner` 顶部告警；FR-035 ROI 按钮 disabled + tooltip "v1.1 规划中"（FR-035b/036/037 全 Won't v1.1）；FR-033 prompt 历史追加（v1 仅列表无 diff viewer，FR-033b Won't v1.1）；FR-038/039 ProcessFiles Zod onChange + 后端 `POST /api/validate/:file` 双层校验，必填空 → `data-invalid="true"` + Save 禁用 + `<CrossFieldErrorList>`；FR-041 二进制 diff `kind:'binary',placeholder:true` 占位不崩；IFR-005 非 git 目录 502 + `{kind:'not_a_git_repo'}` → `<NotAGitRepoBanner role="alert">`；Wave 3 IFR-004 `strict_schema_override: bool|null` 三态控件；coverage line 90.06% / branch 86.53%（≥ 90/80）；vitest 232/232 PASS（55 F22 routes tests + 177 既有）；pytest 651 PASS（含 6 F22 RT01-06 INT real ASGI）；ST cases 18（6 FUNC + 3 BNDRY + 5 UI + 3 SEC + 1 PERF · 16 PASS + 2 BLOCKED — ST-BNDRY-022-001 无二进制 commit fixture / ST-BNDRY-022-002 F20 `_list_git_log` filter 内存空时被忽略，二者均由 vitest 组件级覆盖补齐）；validate_st_cases.py VALID + check_ats_coverage.py OK
- F18 · Bk-Adapter — Agent Adapter & HIL Pipeline (Wave 4 + 4.1 协议层重构 — TUI + Hook Bridge + unified Esc-text，2026-04-27) 覆盖 FR-008/009/011/012/013/015/016/017/018/051/052/053 + NFR-014 + IFR-001/002（FR-014 [DEPRECATED Wave 4]）：HIL 捕获从 stream-json stdout 行解析切换为 `workdir-scoped settings.json hooks` (8 类: PreToolUse(AskUserQuestion) / PostToolUse / SessionStart / SessionEnd + Wave 4.1 Stop / SubagentStop / UserPromptSubmit / Notification) → `POST /api/hook/event` 桥；HIL 回写从 stdin JSON 文本切换为 TUI 键序：默认走 unified Esc-text (`\x1b\x1b[200~<merged_text>\x1b[201~\r`，覆盖 single/multi/multi-question/freeform 一次注入)，baseline `<N>\r` 路径降级为 `HilWriteback(prefer_baseline=True)` 兼容 fallback；`ToolAdapter` Protocol 升级为 7 方法 `build_argv / prepare_workdir / spawn / map_hook_event / parse_result / detect_anomaly / supports`（mypy --strict 0 error）；新增源 `harness/hil/{hook_mapper,tui_keys}.py` + `harness/orchestrator/hook_to_stream.py` + `harness/api/{hook,pty_writer}.py` + `harness/adapter/workdir_artifacts.py` + `harness/cli_dialog/{models,recognizer,decider,actuator,catalog}.py` + `scripts/claude-hook-bridge.py`；物理删除 `harness/hil/extractor.py` + `harness/stream/{parser,banner_arbiter}.py`（FR-014 替代逻辑改走 SessionEnd hook + tool_use_id queue）；`ClaudeCodeAdapter.build_argv` 严格 8 项白名单 `["claude", "--dangerously-skip-permissions", "--plugin-dir", <bundle>, "--settings", <isolated>, "--setting-sources", "project"]`（含 `--model` 时 10 项），永禁 `-p / --print / --output-format / --include-partial-messages / --mcp-config / --strict-mcp-config`；`prepare_workdir` 三件套预置 `<isolated_cwd>/{.claude.json, .claude/settings.json, .claude/hooks/claude-hook-bridge.py(0o755)}`，写路径仅允许 `.harness-workdir/<run-id>/.claude*`，env 白名单不含 `ANTHROPIC_*`，run 前后 `~/.claude/{settings.json,.claude.json}` sha256 字节守恒（NFR-009 强化 + FR-051 AC + IFR-001 AC-w4-1）；`TuiKeyEncoder` 协议（`encode_radio/checkbox/freeform/encode_unified_answer/encode_interrupt`），自由文本拒绝 `\x03 / \x04 / \x1b` 注入（FR-053 SEC）；REST 新增 `POST /api/hook/event`（IAPI-020 · 415 / 422 错误码）+ `POST /api/pty/write`（IAPI-021 · base64 payload）；`HilWriteback._merge_text` 合并文本 + `publish_answered_via_prompt` audit `{value: merged_text, channel: "unified_esc_text"}`；穿刺 v8 实测三方案 (`reference/f18-tui-bridge/evidence-summary.md`) 裁决 unified Esc-text 为默认协议，audit 闭环走 PreToolUse + UserPromptSubmit + Stop（PostToolUse 在 unified 路径下设计预期不 fire）；ST cases 50（FUNC=38 / BNDRY=5 / SEC=5 / PERF=2，47/47 自动化 PASS · validate_st_cases VALID · check_ats_coverage --strict OK）；3 cases PENDING-MANUAL（user-approved 延迟）：ST-FUNC-018-029（T29 真 CLI HIL）/ ST-PERF-018-001（T30 FR-013 20-round PoC ≥95%）/ ST-FUNC-018-038（INT-001 跨特性系统集成 F18+F21+F20+F02）；previous Wave 2 同 ID 条目已被本次 Wave 4 + 4.1 协议层重构整体替换

### Changed
- Increment Wave 2 (refactor-only feature repackaging, 2026-04-24): consolidated 12 fine-grained features into 5 cohesive TDD units — F18 Bk-Adapter (F03+F04+F05), F19 Bk-Dispatch (F07+F08), F20 Bk-Loop (F06+F09+F11), F21 Fe-RunViews (F13+F14), F22 Fe-Config (F15+F16). Preserved F01/F02/F10(st)/F12/F17; F17 dependencies remapped to new ids; 12 old feature ids marked `deprecated:true` for audit traceability. **0 FR/NFR/IFR semantic changes · 0 breaking contracts · 0 code changes** — only Design §4 / §6.2.1 / §11 reorganization, ATS / UCD / guide feature-id remapping, feature-list.json reshape. HIL PoC gate (FR-013) ownership moved F03 → F18.
- **Increment Wave 4** (F18 协议层重构 — TUI + Hook Bridge, 2026-04-27): 响应 F24 hotfix ST 阶段 MiniMax 路由穿刺暴露的 F18 Bk-Adapter 在 claude CLI v2.1.119 上的结构性失效（`--include-partial-messages` 强制 `-p` 模式 + `-p` 与跨 user_turn 多回合 HIL 不可共存）。穿刺 v8 实测证据收录于 `reference/f18-tui-bridge/`（3 轮跨 turn HIL 闭环 + user-scope sha256 不变 + MiniMax 路由生效）。**Breaking changes (内部 IAPI)**：F18 协议层 Stream-JSON → TUI + Hook Bridge；废 IAPI-008（`StreamParser.events`）；MOD IAPI-005（`spawn` 语义 + `prepare_workdir` 前置）/ IAPI-006（`byte_queue` 字段语义降级仅供归档）/ IAPI-007（`HilWriteback` payload 由 JSON → TUI 键序 bytes via IAPI-021）/ IAPI-002 与 `/ws/stream/:ticket_id` envelope rename `StreamEvent → TicketStreamEvent`；新增 IAPI-020（`POST /api/hook/event`）+ IAPI-021（`POST /api/pty/write`）。**Environment requirement bump**：`claude` CLI ≥ 2.1.119（hook stdin schema + TUI 键序在此版本实测稳定）；新增依赖 `<workdir>/.claude/settings.json` 8 字段（env / hooks / enabledPlugins / model / skipDangerousModePermissionPrompt 等）+ `.claude.json` 4 字段（hasCompletedOnboarding / projects.<cwd>.hasTrustDialogAccepted / lastOnboardingVersion / projectOnboardingSeenCount）；写路径白名单仅 `.harness-workdir/<run-id>/.claude*`；新增工件 `harness/api/{hook,pty_writer}.py` + `harness/hil/{hook_mapper,tui_keys}.py` + `harness/orchestrator/hook_to_stream.py` + `harness/adapter/workdir_artifacts.py` + `scripts/claude-hook-bridge.py`。**Deprecation**：FR-014 (`BannerConflictArbiter` 终止横幅 vs 未答 HIL 仲裁) 弃用 — hook 模式下 SessionEnd hook 必触发，时序协调改用 `hook_event_name + tool_use_id` queue；`harness/stream/parser.py` (`JsonLinesParser`) + `harness/hil/extractor.py` (`HilExtractor`) + `harness/stream/banner_arbiter.py` 物理删除（重生为 `HookEventMapper` + `TuiKeyEncoder` + `HookEventToStreamMapper`）。SRS §1.4 ESI 新增（claude CLI 版本 + settings.json/.claude.json 字段依赖清单 + 隔离写路径白名单）；FR-008/009/011/015/016 EARS 重写；FR-051/052/053 新增（prepare_workdir 三件套 / HIL 应答唯一通道 = harness UI / TuiKeyEncoder 协议）；IFR-001 重写；ASM-009/010 新增。Design §4.3 整段重写（4.3.1-4.3.6）+ §4.5 F20 supervisor.py 主循环改造点 + §4.6 F21 TicketStream 数据源 + §4.12 Wave 4 重构清单 + §6.1.1 IFR-001 + §6.2 contracts；env-guide §3 工具表 + §4 §4.5 隔离三件套 (godsuriyel@gmail.com 已审批 v1.2)；ATS §2.1 FR + §2.3 IFR 行重写 / §5.1 INT-001 整段重写 + sha256 守恒 + bracketed paste 字节守恒 SEC + §5.6 Wave 4 增量节。F18/F20 hard reset → failing（待 Worker TDD R-G-R 实现）；F21/F23/F17 仅 wave_note 不重置 status；F19/F22 0 影响。**0 新特性**（全部归 F18 内部 IAPI）。Worker pipeline 由路由器自动接管下一会话从 F18 design 阶段重启。
- Increment Wave 3 (F19 MiniMax OpenAI-compat strict-schema bypass, 2026-04-25): 响应 Session 19 真打证据（MiniMax 端点对 `response_format=json_schema strict` HTTP 400 拒收 → fallback rule，FR-021/023 在该 provider 上等同 disabled）。SRS FR-021 +AC-4/5/6（`ProviderPreset.supports_strict_schema` 能力位 + `ClassifierConfig.strict_schema_override` 三态运行时覆写）；FR-023 +AC-3..7（strict-off 路径：body 不发 `response_format`，system message 末尾追加固定 JSON-only suffix；全 provider 通用容错解析：剥离 `<think>...</think>` + 提取首段语法平衡 JSON；解析失败仍走 RuleBackend 兜底，IAPI-010 永不抛保留）；IFR-004 MODIFY（`response_format` 由 effective_strict 标志条件性发送，URL/method/Authorization 不变）+AC-mod；ASM-008 NEW（部分 OpenAI-compat provider 拒收 strict 子协议，由 `real_external_llm` smoke 验证）。Design §4.4.2/§6.1.4/§6.2.2/§6.2.4/§11.4 同步；ATS L89/L91/L182 Coverage Hint 扩展 + §5.5 Wave 3 changelog；F19 feature design Test Inventory +T47..T52（覆盖 8 新 AC + IFR-004-mod + ASM-008）。**0 breaking contracts**（IAPI-010/IAPI-015/Verdict schema 不变；IAPI-002 / ProviderPreset / ClassifierConfig 字段 Additive 向后兼容）。Worker 流水线（design → tdd → st）已按 Wave 3 实现路径完成：design 一致性复审通过 (`10342f6`)；TDD R-G-R 落 `harness/dispatch/classifier/{models.py · provider_presets.py · llm_backend.py · service.py}` 增量代码（`_extract_json` tolerant parser + `_JSON_ONLY_SUFFIX` + `_effective_strict()` + capability bit），coverage 96.32%/81.45%（≥ 90/80），479/479 regression PASS (`2ba24d4`)；ST cases 61 → 67（+6 条 ST-FUNC-019-047..052 覆盖 T47–T52，119/119 auto PASS · MiniMax `real_external_llm` smoke live 通过 — ASM-008 strict-off 路径生产环境验证成立）(`39edce8`)。

### Fixed
- [Critical] Fix: 打包前 UI↔FastAPI 集成壳 9 项缺陷 + 设计稿控件大幅错位/缺失（B1-B9 合并 · pre-PyInstaller integration QA）(fixes #23) — 用户在 dist 同源（`apps/ui/dist` 经 `harness.api` 静态挂载在 `127.0.0.1:8765`）pre-PyInstaller 手测发现 9 类阻断缺陷，且对照 `docs/design-bundle/eava2/project/{pages,components}/*.jsx` 设计真相源进一步发现 SystemSettings/ProcessFiles/Sidebar 三页面控件大幅错位与缺失（横跨 F01/F12/F21/F22/F23 + `scripts/init_project.py`）。Root cause: 打包前 UI↔FastAPI 集成壳缺乏端到端 QA + 实施未严格对照 design-bundle JSX 真相源 —— F21/F22 frontend IC 实施缺口（B1 RunOverview Start 按钮缺 `onClick`；B5 SystemSettings 5 Tab 标签全英 + `'model'/'auth'` Tab 完全缺设计 SettingsSection + `'classifier'` 缺 `enabled/provider/model_name/api_key_ref` + `'mcp'/'ui'` 仅占位文本；B6 ProcessFiles 缺 file chips/三 h3 分块/右 340px 校验面板/header 双按钮 + fetch 404 卡 loading；B7 Sidebar 缺 `v1.0.0` chip/当前 Run selector dropdown/Runtime status card + collapsed 节点缺 `title`/`aria-label`）+ F21/F23 跨特性契约漂移（B2 hil-inbox/ticket-stream `fetch /api/tickets` 默认无 `run_id` → backend 强制 400；B3 AppShell 单 socket 与 IAPI-001 §6.2.3 5 条独立 WS 路径架构错配）+ F23 production wiring 治理缺口（B4 `StaticFiles html=True` 仅命中精确目录回退 `index.html` → SPA 子路径 404 JSON；B9 `_lifespan` 一次性 `cli_versions/claude_auth` probe 写入 `_health_cache` 永不刷新）+ 外部 plugin installer 将 argparse 标志名（`--version`）/ 子命令名（`status`）当目录写入仓库根而 `scripts/init_project.py` 缺前缀守卫（B8）。Fix（commits `971fb07` TDD-RG + `77d4357` TDD-Refactor + `458dd32` ST B8 in-place + ST 53 cases）—— B1: `useStartRun` mutation 包装 `POST /api/runs/start` + button transition `enabled→disabled+pending`；B2: `useCurrentRunId()` hook 注入 + `enabled: !!currentRunId` skip-fetch + `buildTicketsUrl` 默认 fallback 返 null；B3: 移除 AppShell 根连接，每 channel 自有 `HarnessWsClient` 实例直连具体路径，`assertChannelOrThrow` 5 路径白名单；B4: 显式 catch-all `@app.get("/{full_path:path}")` SPA fallback + 拒 `api/`/`ws/` 前缀 + 拒 path traversal；B5: 5 vertical tabs 中文化 `["模型与 Provider","API Key 与认证","Classifier","全局 MCP","界面偏好"]` + 各 tab 子组件按 `docs/design-bundle/eava2/project/pages/SystemSettings.jsx` 1:1 移植；B6: ProcessFiles 全量重写按 design-bundle JSX —— 顶部 4 file chips + 左 form 三 h3 分块 + features Grid Table + 右 340px ValidationPanel + header 双 button + fetch 404 EmptyState；B7: Sidebar 加 `v1.0.0` chip（`__APP_VERSION__` build-time 注入）+ `<CurrentRunSelector>` + `<RuntimeStatusCard>` + collapsed nav `title`/`aria-label`；B8: `_RESERVED_FLAGS` set + `_validate_safe_arg` 拒 `-` 前缀及保留字 + `_preflight_argv_guard` 在 argparse 之前扫 `argv` 防 flag 注入路径，`scripts/init_project.py` 入口三层防御（B8 守卫 ST 阶段就地补齐 — TDD claim 完成但 commit 未落，SubAgent 监测 6/6 unit FAIL 后修复 6/6 PASS · 仓库根 `--version/`/`status/` 残留 0 命中）；B9: `TTL_SEC = 30.0` `time.monotonic()` 比较 + `_lifespan` 不再 freeze cache + lazy probe，`OSError` 降级返旧值。Coverage 维持 backend line 91.79%/branch 81.04% + frontend line 90.06%/branch 86.53%；vitest 232/232 + pytest 651 + ST 53 cases 全 PASS（41.5% negative · 0 manual · 0 blockers · validate_st_cases VALID · check_ats_coverage --strict OK）。
- [Critical] Fix: F18/F20 IAPI-002 ship miss — 14 REST routes + 5 WS broadcasters + uvicorn ws backend (fixes #20) — F20 ST 仅经 test-only `harness.app.main.build_app()` 工厂验证 IAPI-002/IAPI-019，14 REST + 5 WS endpoints 在 production ASGI 入口 `harness.api:app` 未挂载；并发缺陷 `requirements.txt` 锁 `uvicorn==0.44.0` 未带 `[standard]` extra（缺 `websockets`/`wsproto`），uvicorn 在 ASGI 之前对所有 WS upgrade 直接 HTTP 404。Fix wiring 14 routers + real broadcasters（`RunControlBus`/`HilEventBus`/F18 stream parser/`AnomalyClassifier`/`SignalFileWatcher`）+ `/ws/run/:id` 双定义归一 + `uvicorn[standard]==0.44.0`，恢复 F21 三页面与 F22 进入 TDD 前的真实数据通路（FR-001/024/029/039/042/IFR-007）。
- [Major] Fix: F19 prompts API ↔ F22 §IC Section B 契约对齐（discovered during F22 ST, inline-fixed under user authorization）—— `GET/PUT /api/prompts/classifier` 旧返 `{current: <str>, history: [{rev, saved_at, hash, summary}]}` 与 F22 Zod `classifierPromptSchema` 期望的 `{current: {content, hash}, history: [{hash, content_summary, created_at}]}` 漂移，导致 F22 PromptsAndSkills 页 Zod parse 静默失败 → prompt-history `<li>` 永空。Fix `harness/api/prompts.py` 新增 `_to_f22_ic(prompt)` REST 适配器（GET/PUT 共用），F19 内部 `ClassifierPrompt`/`ClassifierPromptRev` pydantic 模型不动；同步更新 `tests/test_f19_api_routes.py::test_t41/t42` 断言新 shape；vitest 9 + pytest 9 全绿。
- [Major] Fix: F20 `FilesService.read_file_tree` stub → recursive scandir（discovered during F22 ST, inline-fixed under user authorization）—— `harness/api/files.py:71-73` 永远返 `{root, nodes: []}`，F22 DocsAndROI 页文件树永远空；现实现 `Path.rglob('*')` 递归扫描，过滤 `__pycache__/.git/.venv/dist/build/node_modules` 与隐藏文件，返 `{root, entries[], nodes[]}` 双键（`entries[]` 对齐 F22 §IC Section C，`nodes[]` 保 F23 R20 INTG/asgi-rest 测试 backwards-compat）。Path traversal 拦截不变（`?root=../etc` 仍 400）；`docs/plans` 真实返 6 entries；F20/F23 39 PASS。

---

_Format: [Keep a Changelog](https://keepachangelog.com/) — Updated after every git commit._
